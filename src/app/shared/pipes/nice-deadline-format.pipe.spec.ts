import { NiceDeadlineFormatPipe } from './nice-deadline-format.pipe';

describe('NiceDeadlineFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new NiceDeadlineFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
