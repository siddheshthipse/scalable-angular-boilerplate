import { DatePreferencePipe } from './date-preference.pipe';

describe('DatePreferencePipe', () => {
  it('create an instance', () => {
    const pipe = new DatePreferencePipe();
    expect(pipe).toBeTruthy();
  });
});
