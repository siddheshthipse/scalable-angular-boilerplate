import { TimezonePreferencePipe } from './timezone-preference.pipe';

describe('TimezonePreferencePipe', () => {
  it('create an instance', () => {
    const pipe = new TimezonePreferencePipe();
    expect(pipe).toBeTruthy();
  });
});
